# דפוסי בדיקות (TESTING)

**הבדיקה כמקור הבאג.** כל שאר הספרייה מתעדת באגים בקוד. הקובץ הזה
מתעד את המקרה שבו הקוד שבור, הבדיקות ירוקות, ו-**ה-CI הוא זה שמשקר**.

הציר הזה נפרד משאר הספרייה בכוונה. באג ב-`CORE-PATTERNS.md` נתפס
על ידי בדיקה טובה. באג כאן **הוא** הבדיקה — ולכן אי אפשר לתפוס אותו
בעזרת עוד בדיקה מאותו סוג.

**החל תמיד.** אין כאן שום דבר ספציפי ל-stack.

---

## T1. הבדיקה מאמתת את המפרט, לא את הצרכן

**תדירות:** 4 מופעים רצופים במקור אחד (Markdown-Docs MCP)
**חומרה:** HIGH — ביטחון שווא. הפיצ'ר מת והלוח ירוק.

### איך זה נראה

הבדיקה קוראת לקוד דרך ממשק שאף צרכן אמיתי אינו משתמש בו. היא מוכיחה
שהפונקציה לא קורסת. היא **אינה** מוכיחה שהפיצ'ר עובד.

### וריאציות של הדפוס

אותו root cause, ארבעה מופעים שנראים שונה לגמרי. בכל אחד מהם הפער
הוא בין **מה שהבדיקה קוראת** לבין **מה שהמשתמש או הלקוח נוגע בו**:

- **(a) הבדיקה קוראת ל-handler במקום להשתמש בממשק.** הבדיקה שולחת
  `POST` ידני לנתיב, כשהמשתמש בפועל לוחץ על כפתור בטופס. ה-HTML
  מעולם לא נבדק, ולכן טופס שבור עובר.

- **(b) הבדיקה משתמשת בנתיב שהמפרט מגדיר, הלקוח באחר.** המימוש נכון
  לפי המסמך ובכל זאת נכשל, כי הלקוח היחיד שמשתמש בו מתעלם מההצהרה.
  **המפרט אינו הצרכן.**

- **(c) שכבת הבדיקה אינה אוכפת את מה שהסביבה אוכפת.** `httpx` /
  `requests` / `TestClient` אינם דפדפן: אין להם CSP, אין `SameSite`,
  אין CORS, אין same-origin. שום בדיקת שרת לא תתפוס כשל שם.

- **(d) הבדיקה סדרתית לתכונה מקבילית.** קריאה פעמיים ברצף אינה
  מוכיחה אטומיות. הבדיקה עוברת, שתי בקשות מקבילות מצליחות שתיהן.

### דוגמאות אמיתיות

- **Markdown-Docs (#18):** הבדיקות אימתו ש-`tools/list` מחזיר רשימה.
  הלקוח ציפה ל-`401` כדי להתחיל OAuth. הקונקטור הציג "מחובר" ואפס
  כלים.
- **Markdown-Docs (#19):** הבדיקות פנו ל-`/mcp/` עם לוכסן. הלקוח פונה
  ל-`/mcp`, מקבל `307`, וההפניה אינה נושאת `WWW-Authenticate`.
- **Markdown-Docs (#20):** `registration_endpoint` הצהיר על נתיב אחד,
  claude.ai שולח את הרישום ל-`<host>/register` בשורש. הבדיקה עקבה
  אחרי ההצהרה, לא אחרי הלקוח → `404`.
- **Markdown-Docs (#21):** הבדיקה שלחה `POST` ל-`/mcp-consent`
  ישירות. הדפדפן סירב להגיש את הטופס בגלל `form-action 'none'`.
  ארבע בדיקות ירוקות, כפתור מת.
- **Markdown-Docs (קוד הרשאה):** הערה הבטיחה שימוש חד-פעמי, הבדיקה
  קראה פעמיים ברצף ועברה. `asyncio.gather` על אותו קוד החזיר
  `[200, 200]`.

### כלל לזיהוי (להעתקה ל-CLAUDE.md / bugbot)

דווח על כל אחד מהבאים:

1. בדיקה של פיצ'ר עם **ממשק משתמש** (טופס, קישור, כפתור) ששולחת
   בקשה ידנית במקום לגזור אותה מה-HTML/הקומפוננטה שנוצרו בפועל.
2. בדיקה שפונה לנתיב **קבוע במחרוזת**, כשקיים מקור הצהרה בקוד
   (מטא-דאטה, ראוטר, קונפיג) שאפשר לגזור ממנו — או, בכיוון ההפוך,
   כשידוע שהלקוח פונה לנתיב אחר.
3. פיצ'ר שנשען על אכיפה בצד הלקוח (CSP, `SameSite`, CORS,
   `postMessage` origin, service worker) שאין לו בדיקה בדפדפן אמיתי.
4. בדיקה של תכונת מקביליות (single-use, idempotency, lock, rate
   limit) שקוראת ברצף במקום ב-`asyncio.gather` / threads.
5. הערה או docstring שמבטיחים תכונה (`אטומי`, `חד-פעמי`, `idempotent`)
   בלי בדיקה שמאמתת **את התכונה** ולא רק את המסלול המאושר.

### False positives

- פונקציות טהורות בלי צרכן חיצוני — שם הקריאה הישירה **היא** הממשק.
- שכבות פנימיות שנקראות רק מקוד שלנו.
- בדיקות יחידה על לוגיקה, כשקיימת גם בדיקת אינטגרציה שעוברת בממשק.
- כשהצרכן הוא ספרייה שאנחנו נועלים את הגרסה שלה.

### מצב מומלץ

**strict** לכל זרימה שצרכן חיצוני מריץ: OAuth, webhooks, connectors,
טפסים, קריאות API ציבוריות.
**warning** לקוד פנימי.

### איך לתפוס את זה בפועל

שלוש שאלות לפני שכותבים בדיקה לפיצ'ר עם צרכן:

1. **מי הצרכן?** משתמש בדפדפן, תוכנה של מישהו אחר, או קוד שלנו.
2. **דרך איזה ממשק הוא נוגע בזה?** אם התשובה שונה ממה שהבדיקה
   קוראת לו — יש פער.
3. **מה הסביבה שלו אוכפת שהבדיקה לא?** אם התשובה אינה "כלום",
   צריך בדיקה בסביבה ההיא.

כשהצרכן הוא דפדפן, ואין דרך אחרת: **להריץ דפדפן.** Playwright עם
Chromium, פעם אחת, על הזרימה הקריטית. זה תפס באג שארבע בדיקות שרת
לא ראו.

### ראה גם

- `bugbot-rules/test-mirrors-spec-not-client.md`
- `BY-STACK/browser-policy.md` — מה הדפדפן אוכף ושרת לא רואה
- `CORE-PATTERNS.md` U1 — הבדיקה הסדרתית היא הצד השני של TOCTOU

---

## T2. אימות שהבדיקה נופלת על הקוד הישן

**חומרה:** MEDIUM — בדיקה שלא מוכיחה כלום נשארת בסוויטה לנצח

### איך זה נראה

בדיקה נכתבת **אחרי** התיקון. היא עוברת. אף אחד לא בדק אם היא הייתה
נכשלת **לפניו** — ולכן אין ראיה שהיא מכסה משהו.

זה נפוץ במיוחד בבדיקות מקביליות, כי שם קל לכתוב בדיקה שנראית נכונה
ובפועל אינה משחזרת את התנאי.

### דוגמאות אמיתיות

- **Markdown-Docs:** בדיקת ה-race על קוד ההרשאה הורצה על המימוש הישן
  והחזירה `[200, 200, 400, 400]` — כלומר שני טוקנים מאותו קוד. אחרי
  התיקון `[200, 400, 400, 400]`. **בלי ההרצה הזו לא הייתה ראיה.**
- **Markdown-Docs:** בדיקות ה-CSP הורצו על הקוד לפני התיקון ונפלו עם
  `assert "'none'" not in ["'none'"]`.
- **Markdown-Docs:** בגרסה ראשונה ציפיתי שכתובת עם פרטי הזדהות תושמט
  לגמרי. ההרצה הראתה שהיא **מנוקה** ל-origin תקין. ההתנהגות הייתה
  טובה יותר מהציפייה — וזה התגלה רק כי הרצתי.

### כלל לזיהוי

1. בדיקה חדשה שנוספה באותו commit עם התיקון, בלי ציון בגוף ה-PR
   שהיא הורצה על הקוד הקודם ונפלה.
2. בדיקת race / idempotency / lock — **תמיד** לדרוש הרצה על הקוד
   הישן, כי ההסתברות שהבדיקה לא משחזרת את התנאי גבוהה.

### מצב מומלץ

**warning** תמיד. **strict** לבדיקות מקביליות ואבטחה.

---

## T3. תשתית הבדיקות עצמה כמקור לכשל מבלבל

**חומרה:** LOW — לא מגיע לפרודקשן, אבל שורף שעות debug

### איך זה נראה

הכשל נראה כמו באג בספרייה או בקוד, והוא בעצם באופן שהבדיקות מורכבות.

### דוגמאות אמיתיות

- **Markdown-Docs:** `from tests.conftest import mcp_lifespan` — pytest
  מוצא fixtures ב-`conftest.py` לבד, וייבוא מפורש **רושם אותם מחדש**
  עם cache נפרד. fixture עם `scope="session"` רץ פעמיים. התסמין היה
  `StreamableHTTP session manager .run() can only be called once`,
  שנראה כמו באג בספריית ה-MCP.
- **8-projects (#44):** mock של state לא אופס בין בדיקות → בדיקות
  עוברות ונכשלות בלי דטרמיניזם.

### כלל לזיהוי

1. `from ... conftest import` של שם שמעוטר ב-`@pytest.fixture`.
2. mock או singleton עם state שאין לו `reset` ב-fixture teardown.
3. בדיקה שנכשלת רק כשהיא רצה יחד עם אחרות, או רק בסדר מסוים.

### מצב מומלץ

**strict** על ייבוא fixtures מ-conftest — זה תמיד שגוי.

### ראה גם

- `bugbot-rules/test-infra-shared-state.md`
